const endpoint=document.getElementById('endpoint'),secret=document.getElementById('secret'),enabled=document.getElementById('enabled'),status=document.getElementById('status');chrome.storage.local.get(['endpoint','secret','enabled'],v=>{endpoint.value=v.endpoint||'';secret.value=v.secret||'';enabled.checked=v.enabled!==false;});document.getElementById('save').addEventListener('click',()=>{chrome.storage.local.set({endpoint:endpoint.value.trim(),secret:secret.value.trim(),enabled:enabled.checked},()=>{status.textContent='Saved. Keep the Vault page open after you sign in.';});});